---
layout: default
---
<header>
	<h1 class="title">Category: {{ page.title }}</h1>
	{{ content }}
</header>
{% assign category = site.categories[page.title] %}
{% include series.liquid posts=category %}
